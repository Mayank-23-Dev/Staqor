import { SettingsView } from "@/components/settings/SettingsView";

export const metadata = {
  title: "Settings — Staqor",
  description: "Manage your Staqor profile, developer preferences, and code editor settings.",
};

export default function SettingsPage() {
  return <SettingsView />;
}
